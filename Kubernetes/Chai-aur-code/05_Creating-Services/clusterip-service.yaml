apiVersion: v1
kind: Service
metadata:
  name: my-dep-service
spec:
  selector:
    app: dep
  ports:
    - protocol: TCP
      port: 80 # Service Port
      targetPort: 80 # Container Port
  # type: ClusterIP Even if we dont specify type, by default it is ClusterIP